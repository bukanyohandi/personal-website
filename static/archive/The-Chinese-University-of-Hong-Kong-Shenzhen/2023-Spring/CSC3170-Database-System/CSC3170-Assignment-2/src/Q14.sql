SELECT EMPLOYEE_ID, E.JOB_ID, E.DEPARTMENT_ID, DEPARTMENT_NAME
FROM employees E JOIN jobs J JOIN departments D JOIN locations L
WHERE E.JOB_ID = J.JOB_ID AND E.DEPARTMENT_ID = D.DEPARTMENT_ID AND D.LOCATION_ID = L.LOCATION_ID AND L.CITY = "Seattle";